# CUSTOM-DICTIONARY
The Custom Dictionary project implements a key-value store using a linked list. It supports insertion, deletion, search, and display operations with collision handling via chaining. Though less efficient than hash tables, it offers simplicity, educational value, and practical insight into data structures and memory management.
